#!/usr/bin/env ruby

require 'huffshell'
require 'pp'

cs = CommandSuggestor.new
history_filename = '~/.zsh_history'

OhMyZshReader.new(history_filename).
  shell_commands.
  each{ |l| cs.add(l) }

cs.truncate!(30)
puts cs.to_print
