#!/usr/bin/env ruby

require 'huffshel'

tree = ScriptWordTree.new
history_filename = '~/.zsh_history'

OhZshReader.new(history_filename).command_lines.
  each{ |l| tree.add(l) }

tree.truncate!(10)
tree.print!
