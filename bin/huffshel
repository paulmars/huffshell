#!/usr/bin/env ruby

require 'huffshel'

tree = ScriptWordTree.new
history_filename = '~/.zsh_history'

OhMyZshReader.new(history_filename).shell_commands.
  each{ |l| tree.add(l) }

tree.truncate!(10)
tree.print!
