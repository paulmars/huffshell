#!/usr/bin/env ruby

require 'huffshel'
require 'pp'

cs = CommandSuggestor.new
history_filename = '~/.zsh_history'

OhMyZshReader.new(history_filename).
  shell_commands.
  each{ |l| cs.add(l) }

cs.wordtree.truncate!(30)
puts cs.wordtree.to_print
